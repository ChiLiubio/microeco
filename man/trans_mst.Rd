% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_mst.R
\name{trans_mst}
\alias{trans_mst}
\title{Create \code{trans_mst} object for microbial source tracking (MST) analysis.}
\description{
This class is a wrapper for microbial source tracking based on the FEAST algorithm
(Fast Expectation-maximization for microbial Source Tracking; Shenhav et al. 2019, Nature Methods, <doi:10.1038/s41592-019-0431-x>).
It estimates the contribution proportions of potential source environments to sink samples
(e.g., tracking the microbial transfer from bulk soil to rhizosphere soil and then to root).
The class automatically constructs the metadata (Env, SourceSink, id) required by FEAST
from the \code{sample_table}, runs the \code{FEAST} package, and provides ggplot2-based
visualization. Results of multiple runs (e.g., sequential transfer steps) can be stored
in the same object using different labels.
}
\examples{

## ------------------------------------------------
## Method `trans_mst$new`
## ------------------------------------------------

\dontrun{
data(soil_microb)
t1 <- trans_mst$new(dataset = soil_microb, env_col = "Compartment")
}

## ------------------------------------------------
## Method `trans_mst$cal_mst`
## ------------------------------------------------

\dontrun{
data(soil_microb)
t1 <- trans_mst$new(dataset = soil_microb, env_col = "Compartment")
# same sources for all sinks
t1$cal_mst(sources = "Bulk soil", sinks = "Rhizosphere", EM_iterations = 1000, label = "bulk2rhizo")
# different sources for each sink, paired by Plant_ID
t1$cal_mst(sources = c("Bulk soil", "Rhizosphere"), sinks = "Endophyte",
  different_sources_flag = 1, id_col = "Plant_ID", label = "soil2root")
}

## ------------------------------------------------
## Method `trans_mst$cal_mst_chain`
## ------------------------------------------------

\dontrun{
data(soil_microb)
t1 <- trans_mst$new(dataset = soil_microb, env_col = "Compartment")
t1$cal_mst_chain(list(
  list(sources = "Bulk soil", sinks = "Rhizosphere"),
  list(sources = c("Bulk soil", "Rhizosphere"), sinks = "Endophyte",
    different_sources_flag = 1, id_col = "Plant_ID")
))
}

## ------------------------------------------------
## Method `trans_mst$plot_mst`
## ------------------------------------------------

\dontrun{
t1$plot_mst()
t1$plot_mst(use_run = "soil2root", group_sink = "Group")
}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-trans_mst-new}{\code{trans_mst$new()}}
\item \href{#method-trans_mst-cal_mst}{\code{trans_mst$cal_mst()}}
\item \href{#method-trans_mst-cal_mst_chain}{\code{trans_mst$cal_mst_chain()}}
\item \href{#method-trans_mst-plot_mst}{\code{trans_mst$plot_mst()}}
\item \href{#method-trans_mst-print}{\code{trans_mst$print()}}
\item \href{#method-trans_mst-clone}{\code{trans_mst$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-trans_mst-new"></a>}}
\if{latex}{\out{\hypertarget{method-trans_mst-new}{}}}
\subsection{Method \code{new()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{trans_mst$new(dataset = NULL, env_col = NULL, filter_thres = 0)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{dataset}}{\code{\link{microtable}} object.}

\item{\code{env_col}}{default NULL; a column name in \code{sample_table} used as the environment
description (the "Env" information for FEAST), e.g., "Compartment".
If NULL, the environment of each sample is represented by its sample name when
\code{cal_mst} is called with explicit sample vectors (\code{source_samples}/\code{sink_samples}).}

\item{\code{filter_thres}}{default 0; the relative abundance threshold for filtering low-abundance taxa.
Taxa with total relative abundance < filter_thres will be removed.
This is delegated to \code{dataset$filter_taxa(rel_abund = filter_thres)}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
\code{dataset}, \code{sample_table}, \code{otu_table} (taxa x samples counts),
  \code{env_col}, \code{n_samples}, \code{n_taxa} stored in the object.
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
data(soil_microb)
t1 <- trans_mst$new(dataset = soil_microb, env_col = "Compartment")
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-trans_mst-cal_mst"></a>}}
\if{latex}{\out{\hypertarget{method-trans_mst-cal_mst}{}}}
\subsection{Method \code{cal_mst()}}{
Run FEAST source tracking analysis.

Sources and sinks can be specified either by environment values (\code{sources}/\code{sinks},
matched against the \code{env_col} column of \code{sample_table}) or by explicit sample names
(\code{source_samples}/\code{sink_samples}).
The function automatically generates the FEAST metadata:
in the same-sources mode (\code{different_sources_flag = 0}), all sinks share the same source pool;
in the different-sources mode (\code{different_sources_flag = 1}), each sink uses its own source
set defined by the pairing column \code{id_col} (samples sharing the same \code{id_col} value
belong to the same group, e.g., the same plant individual).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{trans_mst$cal_mst(
  sources = NULL,
  sinks = NULL,
  source_samples = NULL,
  sink_samples = NULL,
  different_sources_flag = 0,
  id_col = NULL,
  EM_iterations = 1000,
  COVERAGE = NULL,
  label = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{sources}}{default NULL; character vector of environment values (in the \code{env_col} column)
used as sources, e.g., c("Bulk soil", "Rhizosphere").}

\item{\code{sinks}}{default NULL; character vector of environment values used as sinks, e.g., "Endophyte".}

\item{\code{source_samples}}{default NULL; character vector of sample names used as sources.
Alternative to \code{sources}; cannot be provided together with \code{sources}.}

\item{\code{sink_samples}}{default NULL; character vector of sample names used as sinks.
Alternative to \code{sinks}; cannot be provided together with \code{sinks}.}

\item{\code{different_sources_flag}}{default 0; 0 (FALSE) means all sinks use the same source pool;
1 (TRUE) means each sink uses its own source set paired via \code{id_col}.}

\item{\code{id_col}}{default NULL; a column name in \code{sample_table} defining the pairing groups
for the different-sources mode (required when \code{different_sources_flag = 1}).
Samples sharing the same value are considered paired, e.g., "Plant_ID".
Each pairing group must contain exactly one sink sample; the sources in the group
constitute the source set of that sink.}

\item{\code{EM_iterations}}{default 1000; the number of EM iterations passed to \code{\link[FEAST:FEAST]{FEAST::FEAST}}.}

\item{\code{COVERAGE}}{default NULL; the rarefaction depth. If NULL, the minimum sequencing depth
across all involved source and sink samples is used, so all samples are uniformly downsampled.}

\item{\code{label}}{default NULL; the label for storing the result, e.g., "bulk2rhizo".
If NULL, a label like "run1", "run2", ... is generated automatically.
Reusing an existing label overwrites the previous result with the same label.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
\code{res_mst[[label]]}: Sink x Source contribution proportions matrix
  (rownames: sink sample names; colnames: source sample names + "Unknown"; each row sums to 1);
  \code{res_mst_env[[label]]}: contributions aggregated by source environment;
  \code{res_mst_param[[label]]}: parameters and sample information of the run.
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
data(soil_microb)
t1 <- trans_mst$new(dataset = soil_microb, env_col = "Compartment")
# same sources for all sinks
t1$cal_mst(sources = "Bulk soil", sinks = "Rhizosphere", EM_iterations = 1000, label = "bulk2rhizo")
# different sources for each sink, paired by Plant_ID
t1$cal_mst(sources = c("Bulk soil", "Rhizosphere"), sinks = "Endophyte",
  different_sources_flag = 1, id_col = "Plant_ID", label = "soil2root")
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-trans_mst-cal_mst_chain"></a>}}
\if{latex}{\out{\hypertarget{method-trans_mst-cal_mst_chain}{}}}
\subsection{Method \code{cal_mst_chain()}}{
Run sequential source tracking steps (e.g., bulk soil -> rhizosphere soil -> root).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{trans_mst$cal_mst_chain(steps = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{steps}}{default NULL; a list where each element is a list of arguments
passed to \code{cal_mst} (without \code{label}), e.g.,
\code{list(list(sources = "Bulk soil", sinks = "Rhizosphere"),
list(sources = c("Bulk soil", "Rhizosphere"), sinks = "Endophyte"))}.
Each step is stored in \code{res_mst[["step1"]]}, \code{res_mst[["step2"]]}, ...
unless a custom \code{label} is provided inside the step.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
results stored in \code{res_mst}, \code{res_mst_env} and \code{res_mst_param}.
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
data(soil_microb)
t1 <- trans_mst$new(dataset = soil_microb, env_col = "Compartment")
t1$cal_mst_chain(list(
  list(sources = "Bulk soil", sinks = "Rhizosphere"),
  list(sources = c("Bulk soil", "Rhizosphere"), sinks = "Endophyte",
    different_sources_flag = 1, id_col = "Plant_ID")
))
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-trans_mst-plot_mst"></a>}}
\if{latex}{\out{\hypertarget{method-trans_mst-plot_mst}{}}}
\subsection{Method \code{plot_mst()}}{
Plot the source tracking result as a stacked barplot.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{trans_mst$plot_mst(
  use_run = NULL,
  use_env = TRUE,
  group_sink = NULL,
  show_unknown = TRUE,
  colors = NULL,
  unknown_color = "grey80",
  legend_title = NULL,
  bar_width = 0.8,
  xtext_size = 10,
  xtext_angle = 45,
  facet_scales = "free_x"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{use_run}}{default NULL; the label (or index) of the run stored in \code{res_mst}.
If NULL, the first run is used.}

\item{\code{use_env}}{default TRUE; whether to use the environment-level aggregated contributions
(\code{res_mst_env}). If FALSE, the sample-level contributions (\code{res_mst}) are shown.}

\item{\code{group_sink}}{default NULL; a column name in \code{sample_table} used to group the sinks.
The plot is facetted by this column and the sinks are ordered accordingly, e.g., "Group".}

\item{\code{show_unknown}}{default TRUE; whether to show the "Unknown" contribution.}

\item{\code{colors}}{default NULL; a vector of fill colors for the sources.
If NULL, the Dark2 (environment level) or Paired (sample level) palette of RColorBrewer is used.}

\item{\code{unknown_color}}{default "grey80"; the fill color for the "Unknown" source.}

\item{\code{legend_title}}{default NULL; the legend title. If NULL, "Source" is used.}

\item{\code{bar_width}}{default 0.8; the bar width passed to \code{\link{geom_bar}}.}

\item{\code{xtext_size}}{default 10; x-axis text size.}

\item{\code{xtext_angle}}{default 45; x-axis text angle.}

\item{\code{facet_scales}}{default "free_x"; the scales parameter of \code{\link{facet_wrap}}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
ggplot2 plot.
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
t1$plot_mst()
t1$plot_mst(use_run = "soil2root", group_sink = "Group")
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-trans_mst-print"></a>}}
\if{latex}{\out{\hypertarget{method-trans_mst-print}{}}}
\subsection{Method \code{print()}}{
Print the trans_mst object.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{trans_mst$print()}\if{html}{\out{</div>}}
}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-trans_mst-clone"></a>}}
\if{latex}{\out{\hypertarget{method-trans_mst-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{trans_mst$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
